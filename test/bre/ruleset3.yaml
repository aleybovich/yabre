name: ruleset3

require:
  - settings
  - scripts

conditions:
  execute_ruleset3:
    check: |
      function() {
        return true
      }
    true:
      description: Executing ruleset3 - terminating
      action: |
        function() {
          debug(executeRuleSet3(context));
        }
      terminate: true
