name: ruleset1

require:
  - settings
  - scripts

conditions:
  execute_ruleset1:
    description: Execute ruleset1
    check: |
      function() {
        return true
      }
    true:
      description: Executing ruleset1 - terminating
      action: |
        function() {
          debug(executeRuleSet1(context));
        }
      terminate: true
