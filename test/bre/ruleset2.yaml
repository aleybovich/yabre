name: ruleset2

require:
  - settings
  - scripts

conditions:
  execute_ruleset2:
    check: |
      function() {
        return true
      }
    true:
      description: Executing ruleset2 - terminating
      action: |
        function() {
          debug(executeRuleSet2(context));
        }
      terminate: true
